<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book Store</title>
  <style>
   body {
       font-family: Arial,sans-serif;
        margin:0; 
        background:#f4f6f9; 
      }
  header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: linear-gradient(90deg, #d8c4c9, #ff9671);
    color: white;
    padding: 15px 20px;
    position: sticky;
    top: 0;
    z-index: 1000;
  }

  header h1 {
    margin: 0;
    font-size: 20px;
  }

  header h1 a:hover {
    text-decoration: underline;
  }

header a button {
  background: linear-gradient(90deg,#28a745,#20c997); 
  border: none;            
  border-radius: 6px;      
  color: white;            
  cursor: pointer;          
  padding: 8px 16px;     
  font-size: 14px;          
  font-weight: bold;
  transition: 0.3s;        
}
header a button:hover {
  background: linear-gradient(90deg,#20c997,#28a745); 
  box-shadow: 0 4px 8px rgba(0,0,0,0.2);            
}

 /* card */
.card-container { 
      display:flex; 
      flex-wrap:wrap;
      justify-content:center; 
      gap:20px; 
      margin:30px 20px;
    }
    .card { 
      border:1px solid #ccc;
      border-radius:8px; 
      padding:15px; 
      width:260px; 
      background:white; 
      box-shadow:0 2px 6px rgba(0,0,0,0.15);
    }
    .card img {
      width: 100%;
      height: 250px;       
      border-radius: 8px;
      object-fit: cover;   
      margin-bottom: 12px;
      display: block;
    }
    .card h3 {
       margin:0; 
       font-size:18px; 
       color:#333;
       text-align: center; 
      }
    .card p { 
      margin:6px 0; 
      font-size:14px; 
      color:#555; 
    }
    .actions { 
      margin-top:12px; 
      display:flex; 
      justify-content:space-between; 
    }
    .edit-btn { 
      background:#007bff; 
      color:white; 
      border:none;
       padding:6px 10px;
       border-radius:5px; 
       cursor:pointer; 
      }
    .delete-btn { 
      background:#dc3545; 
      color:white; 
      border:none; 
      padding:6px 10px;
      border-radius:5px; 
      cursor:pointer;
     }

     /* banner */
  .banner {
    background: url('https://static.vecteezy.com/system/resources/thumbnails/036/594/342/small_2x/ai-generated-book-store-advertisment-background-with-copy-space-free-photo.jpg') no-repeat center center/cover;
    height: 400px;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    color: white;
    position: relative;
  }
  .banner::after {
    content: "";
    position: absolute;
    top: 0; left: 0;
    width: 100%; height: 100%;
    background: rgba(0,0,0,0.5);
  }

  .banner-content {
    position: relative;
    z-index: 1;
    max-width: 700px;
    padding: 20px;
  }

  .banner h2 {
    font-size: 32px;
    margin-bottom: 10px;
    letter-spacing: 1px;
  }

  .banner p {
    font-size: 18px;
    margin-bottom: 20px;
  }

  .banner-btn {
    padding: 10px 20px;
    font-size: 16px;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    background: linear-gradient(90deg,#28a745,#20c997);
    color: white;
    font-weight: bold;
    transition: 0.3s;
  }

  .banner-btn:hover {
    background: linear-gradient(90deg,#20c997,#28a745);
    box-shadow:0 4px 10px rgba(0,0,0,0.3);
  }
  h2{
     text-align: center;
     margin-top: 40px;
     margin-bottom: 40px;
  }
  /* footer */
  .footer {
    background: linear-gradient(90deg, #d8c4c9, #ff9671);
    color: white;
    text-align: center;
    padding: 25px 20px;
    margin-top: 40px;
  }

  .footer-content h2 {
    margin: 0 0 10px 0;
    font-size: 22px;
    letter-spacing: 1px;
  }

  .footer-content p {
    margin: 5px 0;
    font-size: 14px;
  }

  @media(max-width: 768px) {
    .footer-content h2 {
      font-size: 18px;
    }
    .footer-content p {
      font-size: 12px;
    }
  }
  </style>
</head>
<body>

<header>

  <h1><a href="/" style="text-decoration:none; color:white;">📚 My Book Store</a></h1>
  <a href="/add-book"><button type="button">Add Book</button></a>
  
</header>


<!-- Banner Section -->
<section class="banner">
  <div class="banner-content">
    <h2>Welcome to My Book Store</h2>
    <p>Find your favorite books, explore genres, and start reading today 📖</p>
    <a href="/add-book"><button class="banner-btn">explore</button></a>
  </div>
</section>
<!-- Book Cards -->

<div>
  <h2>BOOK LIST</h2>
</div>
<div class="card-container" id="bookContainer">
  <% if (books.length > 0) { %>
    <% books.forEach((book) => { %>
      <div class="card" data-id="<%= book._id %>">
        <img src="<%= book.coverImage %>" alt="Book Cover" />
        <h3><%= book.title %></h3>
        <p><b>Author:</b> <%= book.author %></p>
        <p><b>Genre:</b> <%= book.genre %></p>
        <p><b>Price:</b> ₹<%= book.price %></p>
        <p><b>Year:</b> <%= book.year %></p>
        <div class="actions">
          <a href="/edit-book/<%= book._id %>"><button class="edit-btn">Edit</button></a>
          <a href="/delete-book/<%= book._id %>"><button class="delete-btn">Delete</button></a>
        </div>
      </div>
    <% }) %>
  <% } else { %>
    <p>No books found.</p>
  <% } %>
</div>
<!-- Footer Section -->
<footer class="footer">
  <div class="footer-content">
    <h2>📚 My Book Store</h2>
    <p>Find your favorite books and start reading today.</p>
    <p>© <%= new Date().getFullYear() %> My Book Store. All Rights Reserved.</p>
  </div>
</footer>
</body>
</html>

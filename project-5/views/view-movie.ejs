<!DOCTYPE html>
<html>
<head>
  <title><%= movie.title %> | Book My Show</title>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">

  <style>
 
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #141E30, #243B55);
      margin: 0;
      padding: 0;
      color: #333;
    }

    /* ===== Header ===== */
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: rgba(0, 0, 0, 0.9);
      color: white;
      padding: 15px 40px;
      position: sticky;
      top: 0;
      z-index: 1000;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4);
    }

    header a {
      color: white;
      text-decoration: none;
      font-weight: 600;
      font-size: 18px;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    header a:hover {
      color: #ff4757;
      transform: translateX(-3px);
    }
    .container {
      max-width: 1100px;
      margin: 60px auto;
      background: rgba(255, 255, 255, 0.97);
      border-radius: 15px;
      padding: 30px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
      display: flex;
      gap: 40px;
      align-items: flex-start;
      transition: all 0.4s ease;
    }

    .container:hover {
      transform: scale(1.01);
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.25);
    }
    .poster {
      flex: 1;
      position: relative;
    }

    .poster img {
      width: 100%;
      height: 480px;
      object-fit: cover;
      border-radius: 15px;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.4);
      transition: transform 0.4s ease;
    }
    .movie-info {
      flex: 2;
    }

    .movie-info h2 {
      font-size: 32px;
      color: #111;
      margin-bottom: 10px;
      border-bottom: 3px solid #ff4757;
      display: inline-block;
      padding-bottom: 5px;
    }
    .movie-info p {
      font-size: 16px;
      color: #444;
      margin: 8px 0;
      line-height: 1.6;
    }

    .movie-info p strong {
      color: #000;
      font-weight: 600;
    }

    /* ===== Buttons ===== */
    .btn-group {
      margin-top: 25px;
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 10px 20px;
      border-radius: 8px;
      font-weight: 600;
      text-decoration: none;
      font-size: 15px;
      transition: all 0.3s ease;
      cursor: pointer;
    }

    .book-btn {
      background: #ff4757;
      color: white;
    }

    .book-btn:hover {
      background: #e84118;
      transform: translateY(-2px);
    }

    .back-btn {
      background: #222;
      color: white;
    }

    .back-btn:hover {
      background: #333;
      transform: translateY(-2px);
    }

    /* ===== Responsive Design ===== */
    @media (max-width: 900px) {
      .container {
        flex-direction: column;
        align-items: center;
      }

      .poster img {
        width: 100%;
        height: 400px;
      }

      .movie-info {
        width: 100%;
        text-align: center;
      }

      .btn-group {
        justify-content: center;
      }
    }
  </style>
</head>

<body>

<header>
  <a href="/"><i class="ri-arrow-left-line"></i> Back to Movies</a>
</header>

<div class="container">
  <div class="poster">
    <% if(movie.poster){ %>
      <img src="/<%= movie.poster %>" alt="<%= movie.title %>">
    <% } %>
  </div>

  <div class="movie-info">
    <h2><%= movie.title %></h2>

    <p><strong>Genre:</strong> <%= movie.genre %></p>
    <p><strong>Year:</strong> <%= movie.year %></p>
    <% if(movie.releaseDate){ %>
      <p><strong>Release Date:</strong> <%= new Date(movie.releaseDate).toLocaleDateString() %></p>
    <% } %>
    <% if(movie.director){ %>
      <p><strong>Director:</strong> <%= movie.director %></p>
    <% } %>
    <% if(movie.language){ %>
      <p><strong>Language:</strong> <%= movie.language %></p>
    <% } %>
    <% if(movie.description){ %>
      <p><strong>Description:</strong> <%= movie.description %></p>
    <% } %>

    <div class="btn-group">
      <a href="/" class="back-btn"><i class="ri-arrow-left-line"></i> Back</a>
    </div>
  </div>
</div>

</body>
</html>